#include <cstring>

// Given a specified str, compute its overlay function, stored in table t_overlay[]

void compute_overlay(char* str, int t_overlay[])
{
	if(str == NULL)
		return;

	int i;
	len = strlen(str);


	for(i=0; i<len; ++i)
		t_overlay[i] = -1;
	
	for(i=1; i<len; ++i)
	{
		
	}

}
